#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <math.h>
#include <time.h>

//was meant to be the actual wave one, but it seemed that it will take a long time to I just gave up the idea.
/*void waveIchi(int monsterTypeZomebieATK, int monsterTypeZomebieHP, int zombie, int player, int bullets, int playerHP, int playerATK){

    for(int i = 0; i < bullets; i++){
        for(int i = 0; i < zombie; i++){
        monsterTypeZomebieHP -= playerATK;
        playerHP -= monsterTypeZomebieATK;
        }
        if(bullets == 0 || playerHP == 0 && player == 1){
            break;
        }
    }

    if(monsterTypeZomebieHP = 0){
        zombie--;
    }else if(playerHP = 0 && player == 2){
        printf("You lose one player");
        player--;
        playerHP = 100;
    }else if(playerHP = 0 && player == 1){
        printf("You lose.");
    }


    return bullets;
}*/

